# Текущее состояние
Обновлено: 2026-05-12

## 🎯 Глобальная цель
Реализовать локального Telegram-бота (YTS_bot) с поддержкой загрузки медиа (yt-dlp), транскрибации (Groq Whisper), LLM-аналитики (Omniroute) и базы знаний (ChromaDB), используя модульную архитектуру (Feature Toggles).

## ✅ Сделано
- [x] Проектирование архитектуры и потоков данных.
- [x] Составление базовых правил в `CLAUDE.md`.
- [x] Написание `README.md` с инструкциями для запуска и отложенного тестирования.

## 🚀 В работе (Фаза 1: Core & MVP Загрузчик)
- [ ] **1. Базовая инфраструктура:** Создать `core/config.py` (считывание `.env` и Feature Toggles) и `core/logger.py`.
- [ ] **2. Тестовый скрипт:** Написать `run_tests.py` для диагностики локальной системы (поиск ffmpeg, проверка токенов Telegram).
- [ ] **3. Каркас бота:** Инициализировать `aiogram 3.x` в `main.py` и создать базовые хэндлеры (`bot/handlers/start.py`).
- [ ] **4. Модуль yt-dlp:** Написать `services/downloader.py`. Реализовать асинхронное получение метаданных (название, канал) без скачивания файла.
- [ ] **5. Интерактивный UI:** Создать `bot/keyboards/main_menu.py`, который рендерит кнопки только для включенных в `config` модулей.

## ⏭ Следующий шаг
1. Написать `core/config.py` и `.env.example`.
2. Создать диагностический скрипт `run_tests.py`.

## 📚 Читать перед работой
- `CLAUDE.md` (строгие правила: асинхронность, Feature Toggles, логирование)
- `README.md` (концепция проекта)

---
## 📦 Бэклог (Будущие фазы)

**Фаза 2: Smart Processing (Транскрибация и LLM)**
- [x] Интеграция Whisper API: Groq (основной) + OmniRoute (fallback), выбор модели.
- [x] Логика нарезки больших аудио (> 25МБ) через `ffmpeg` (`utils/media_chunker.py`).
- [ ] Интеграция Omniroute / Kiro AI (Claude/Haiku) в `services/llm_router.py`.

**Фаза 3: Storage & Knowledge Base (Память)**
- [ ] Локальная векторная база ChromaDB (`services/knowledge_base.py`).
- [ ] Автоматический чанкинг транскриптов.
- [ ] Тихая загрузка файлов через Google Drive API (`services/gdrive.py`).

**Фаза 4: Export & Polish**
- [ ] Генерация `.md` отчетов с саммари.
- [ ] Полноценный семантический поиск по базе бота.
