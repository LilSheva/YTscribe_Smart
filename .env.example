# ============================================
# YTS_bot — Шаблон переменных окружения
# Скопируйте этот файл как .env и заполните значения
# cp .env.example .env
# ============================================

# --- Telegram ---
BOT_TOKEN=123456789:AABBccDDeeFFggHHiiJJkkLLmmNNooPPqqRR

# --- API Keys ---
GROQ_API_KEY=gsk_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
OMNIROUTE_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
# Base URL for LLM API (OpenAI-compatible endpoint)
# OpenRouter: https://openrouter.ai/api/v1
# OmniRoute local: http://localhost:20128/v1
OMNIROUTE_BASE_URL=https://openrouter.ai/api/v1

# --- bot_service.bat (optional) ---
# POT_SERVER_DIR=C:\Users\yakov\bgutil-ytdlp-pot-provider\server
# POT_PORT=4416
# OMNIROUTE_PORT=20128   — только проверка, что приложение OmniRoute запущено

# --- Транскрибация (Speech-to-Text) ---
# Основной провайдер: groq (бесплатно, быстро)
# Fallback: omniroute (через OpenRouter/другие провайдеры)
TRANSCRIPTION_PROVIDER=groq
# Модель Whisper (доступные: whisper-large-v3, whisper-large-v3-turbo, distil-whisper-large-v3-en)
# whisper-large-v3-turbo — оптимально для RU+EN (быстрая, качественная)
WHISPER_MODEL=whisper-large-v3-turbo
# Максимальный размер файла для Groq free tier (MB)
WHISPER_MAX_FILE_MB=25

# --- Google Drive ---
# Режим local: папка клиента Google Drive на ПК (рекомендуется для .md)
# GDRIVE_MODE=local
# GDRIVE_LOCAL_DIR=G:/My Drive/YTscribe/transcripts
# Режим api: OAuth + Drive API (legacy, для m4a/mp4 через API)
# GDRIVE_MODE=api
GDRIVE_CREDENTIALS_PATH=credentials/gdrive_service.json
GDRIVE_MEDIA_FOLDER_ID=
GDRIVE_TRANSCRIPTS_FOLDER_ID=

# --- Медиа ---
# Браузер для извлечения cookies (chrome, firefox, edge, brave)
BROWSER_FOR_COOKIES=chrome
# Папка для временных файлов (по умолчанию: temp/)
TEMP_DIR=temp

# --- Feature Toggles (True / False) ---
# Отключение модуля = кнопки не рендерятся, вызовы возвращают заглушку
ENABLE_DOWNLOADER=True
ENABLE_TRANSCRIPT=True
ENABLE_LLM=True
ENABLE_KB=False
ENABLE_GDRIVE=True

# --- LLM Settings ---
LLM_MODEL=claude-3-5-sonnet-20241022
LLM_MAX_TOKENS=4096
# Авто-саммари сразу после транскрибации (инсайты + вывод)
AUTO_LLM_SUMMARY=True
# Локальный кеш LLM (одинаковый транскрипт + промпт + модель → без повторного запроса)
LLM_CACHE_ENABLED=True
LLM_CACHE_MAX_ENTRIES=500

# --- Knowledge Base (внешний сервис, на будущее) ---
KB_API_URL=

# --- GDrive sync (optional) ---
# GDRIVE_SYNC_ON_START=True
# GDRIVE_SYNC_AUTO_REPAIR=False

# --- Chat output (per-user overrides in bot settings) ---
SHOW_TRANSCRIPT_IN_CHAT=False
SHOW_LLM_IN_CHAT=True

# --- Predictive progress ---
# TIMING_MIN_SAMPLES=5
# TIMING_RECENT_K=10

# --- Logging ---
# Уровень: DEBUG, INFO, WARNING, ERROR
LOG_LEVEL=INFO
